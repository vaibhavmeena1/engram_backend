---
description: Extract durable Engram facts from visible or pasted old-session history.
argument-hint: [optional focus, e.g. user|repo|org or topic]
---

Load and execute the `engram-extract` skill. Review the visible conversation above or pasted transcript and save only durable facts that pass the memory-quality rubric. Do not ask for user/org IDs.

$ARGUMENTS