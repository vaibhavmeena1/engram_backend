---
description: Show current Engram MCP user, org, and repository resolution.
---

Load and execute the `engram-status` skill. Verify the current MCP connection and repository context using `get_current_context` without user/org input.