---
description: Retrieve relevant Engram context for the current request across user, repo, and org memory.
argument-hint: <question or topic>
---

Load and execute the `engram-context` skill for this request. User/org context comes from auth; repository context comes from hooks.

$ARGUMENTS